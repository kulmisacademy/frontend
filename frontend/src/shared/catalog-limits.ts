/** Default page size for catalog API and marketplace (server-side pagination). */
export const CATALOG_PAGE_SIZE = 20;

/** Hard cap for `limit` query (matches Express zod). */
export const CATALOG_MAX_LIMIT = 100;
